name: coinbase_commerce
description: A wrapper for coinbase_commerce api to enable cryptocurrency transactions.
version: 0.0.1
homepage: https://github.com/onuohasilver/coinbase_commerce

environment:
  sdk: '>=2.12.0 <3.0.0'
  flutter: ">=1.17.0"
  

dependencies:
  flutter:
    sdk: flutter
  html: ^0.14.0+4
  http: ^0.13.1

dev_dependencies:
  flutter_test:
    sdk: flutter

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

